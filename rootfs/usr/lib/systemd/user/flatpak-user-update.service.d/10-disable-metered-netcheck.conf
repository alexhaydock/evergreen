[Service]
# For some reason this service ships with an ExecCondition that checks NetworkManager
# to see whether we're on a metered connection before launching. I'm not sure it's
# working correctly though. Maybe a race condition when the machine is waking from
# sleep that stops it firing if we're firing because of Persistent=true ?
ExecCondition=
